streamlit>=1.32
pandas>=2.0
scikit-learn>=1.3
nltk>=3.8
plotly>=5.18
joblib>=1.3

# AI Chat Assistant (optional — app runs fine without these, the chat
# page just shows a setup message until they're installed + configured)
langgraph>=0.2
langchain>=0.3
langchain-huggingface>=0.1
python-dotenv>=1.0
